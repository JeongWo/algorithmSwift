import Foundation

func inputSolution17() {
    
    var inputLines: [String] = []
    
    while let line = readLine() {
        inputLines.append(line)
    }
    
    for line in inputLines {
        print(line)
    }
}
